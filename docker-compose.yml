services:
  dashboard:
    build: .
    container_name: fifa2026_dashboard
    ports:
      - "8050:8050"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    restart: unless-stopped
